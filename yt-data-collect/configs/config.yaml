pipeline:
  query: "python tutorial"
  limit: 10
  base_output_dir: "outputs"

search:
  upload_days: 365
  max_limit: 50
  sort_by: "view_count"

subtitles:
  languages:
    - "en"
  max_workers: 4

logging:
  level: "INFO"